<UserControl x:Class="WPFWeather.Views.SetLocation.SetZipCodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFWeather.Views.SetLocation"
			 xmlns:vmsSL="clr-namespace:WPFWeather.ViewModels.SetLocation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid d:DataContext="{d:DesignInstance vmsSL:SetZipCodeViewModel}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="3*"/>
			<ColumnDefinition Width="10"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="10"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<TextBox Grid.Column="0" VerticalAlignment="Top" Text="{Binding ZipCode, UpdateSourceTrigger=PropertyChanged}"/>
		<TextBox Grid.Column="2" VerticalAlignment="Top" Text="{Binding CountryCode, UpdateSourceTrigger=PropertyChanged}"/>
		<Button Grid.Column="4" Command="{Binding SumbitCommand}" VerticalAlignment="Top">Sumbit</Button>
	</Grid>
</UserControl>
